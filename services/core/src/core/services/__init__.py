"""Use-case'ы core. См. SPEC §11.2.

Каждый use-case — это функция/класс, принимающий доменное событие шины и
зависимости (repositories), и возвращающий результат — обычно список команд
шины для публикации. Use-case не вызывает шину напрямую: публикация — забота
handler-обёртки (см. ``core.handlers``).
"""
